<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gas.geny.com.companion">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/das_logo_1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Aga"
            android:label="AGA: Home"
            android:theme="@style/Aga" />
        <activity
            android:name=".launcher"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Home"
            android:label="Comapanion: Home"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".Donate"
            android:label="Donations"
            android:theme="@style/Dona">

        </activity>
        <activity android:name=".RehabCenter">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RehabFirst"></activity>
        <activity android:name=".post"></activity>
        <activity android:name=".CreateGroup"></activity>
    </application>

</manifest>